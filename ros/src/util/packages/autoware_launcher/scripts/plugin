#! /usr/bin/env python

import os
from autoware_launcher.core.plugin import AwPluginLoader

package_path = os.path.dirname(__file__)
package_path = os.path.join   (package_path, "..")
package_path = os.path.abspath(package_path)

plugin = AwPluginLoader(os.path.join(package_path, "plugins"))