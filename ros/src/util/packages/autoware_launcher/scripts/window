#! /usr/bin/env python

import os
import sys

from autoware_launcher import AwSchemaNode
from autoware_launcher import AwApplication
from autoware_launcher import AwMainWindow
from autoware_launcher import AwNodeWidget
from autoware_launcher import AwRootConfigWidget

awtree = AwSchemaNode.load(os.path.join(os.path.dirname(__file__), "../plugins/"))
print("============================================================")
awtree.dump()
print("============================================================")

application = AwApplication(sys.argv)
window = AwMainWindow()
widget = AwNodeWidget(window, awtree)
window.setCentralWidget(widget)
window.show()
sys.exit(application.exec_())
